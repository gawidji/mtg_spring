package com.example.demo.utils;

/*
import java.util.Collection;

import org.springframework.security.core.Authentication;
import org.springframework.security.core.GrantedAuthority;

import com.doranco.security.enums.EnumRole;
import com.example.demo.enums.UserRole;

public class UserRoleExtractor {
	
	static public boolean isAdmin(Authentication authentication) {
		boolean isAdmin = false;
		Collection <? extends GrantedAuthority> authorities = authentication.getAuthorities();
		for (GrantedAuthority authority : authorities) {
			if(authority.getAuthority().equals(UserRole.ADMIN.toString()))
				isAdmin = true;
		}
		return isAdmin;
		
	}
	
	static public boolean isUser(Authentication authentication) {
		boolean isUser = false;
		Collection <? extends GrantedAuthority> authorities = authentication.getAuthorities();
		for (GrantedAuthority authority : authorities) {
			if(authority.getAuthority().equals(UserRole.USER.toString()))
				isUser = true;
		}
		return isUser;
		
	}

}
*/
